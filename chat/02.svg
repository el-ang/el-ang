<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 162 34">
    <style>
        .res {
            --bg: #6424ff;
        }

        .send {
            --bg: #ff3636;
        }

        .out.bubble {
            fill: var(--bg);
        }

        .chat.bubble {
            rx: 4;
            ry: 4;
            fill: var(--bg);
        }
        
        .chat.text {
            font-family: sans-serif;
            font-size: 12px;
            fill: #fff;
        }
    </style>
    <path class="out bubble res" d="M 0,0 L 8,0 L 4,4 Z"/>
    <rect class="chat bubble res" x="4" y="0" width="154" height="34"/>
    <text class="chat text" x="8" y="13">i thought i'll just need ATS</text>
    <text class="chat text" x="8" y="28">friendly format for that case</text>
</svg>